---
title: Agent 复利
created: 2026-07-21
updated: 2026-07-21
type: concept
tags: [agent, memory, skills, compound-interest]
sources: [raw/articles/llm-agent-compound-interest.md]
confidence: high
---

# Agent 复利

AI 工具的价值不在单次对话的质量，而在跨会话的累积。没有持久化的 AI 是打工；有持久化的 AI 是投资。

## 复利定义

金融复利：收益产生收益，指数增长。Agent 复利：每次交互产生的知识（记忆、技能、自动化）让下次交互更快、更准、更省 token。

## 复利基础设施

| 机制 | 作用 | 复利路径 |
|------|------|---------|
| **Memory** | 跨会话持久化用户偏好和纠正 | 第 1 次纠正 → 永久生效 → 节省未来 N 次重复 |
| **Skills** | 复杂任务的结晶，可被调用 | 第 1 次探索 → 写成 skill → 下次跳过探索直接执行 |
| **Cron** | 定时自动化任务 | 第 1 次配置 → 永久零成本运行 |
| **Session Search** | 历史决策可检索复用 | 过去的判断 → 站在上面继续 → 不从零开始 |

## 与其他 Agent 的对比

| 维度 | ChatGPT | Claude Projects | Cursor | Hermes |
|------|---------|----------------|--------|--------|
| 跨会话记忆 | ❌ 每次重来 | ⚠️ 有限 | ❌ | ✅ memory 注入每轮 |
| 技能积累 | ❌ | ❌ | ❌ | ✅ skill 库 |
| 自动化 | ❌ | ❌ | ❌ | ✅ cron 持久运行 |
| 纠错闭环 | ❌ 下次重犯 | ❌ 下次重犯 | ❌ | ✅ 纠正→memory→永不重犯 |

**本质区别**：其他 Agent 是**无状态函数**（输入→输出→忘记）。Hermes 是**有状态系统**（输入→输出→记忆→下次更快）。

## 转折点

```
专用工具：T(n) = C_d          （每轮固定成本，线性）
Hermes：  T(n) = C₀ + K/e^(n/k) （初始高，随累积衰减）
```

| 阶段 | 专用工具 | Hermes | 谁赢 |
|------|---------|--------|------|
| 第 1-5 次 | 直接执行，零开销 | 加载 memory，可能误用 | 专用工具 |
| 第 10-20 次 | 不变 | memory 积累，纠正生效 | 持平 |
| 第 50+ 次 | 不变 | skills 已有，自动化已配 | Hermes |

**转折点取决于任务特性**：高重复→早（~10 次），一次性→永不转折，高关联→最早。

## 数据主权

复利有隐含前提：**积累的知识必须归用户所有**。否则复利只是为平台打工。

```
能自主进化 + 数据在云端 = 平台锁定
不能自主进化 + 数据在本地 = 无复利
能自主进化 + 数据在本地 = Hermes（复利归用户）
```

## 直线 vs 支线任务

| 维度 | 直线任务 | 支线任务（delegate） |
|------|---------|-------------------|
| 会话上下文 | ✅ 持续积累 | ❌ 每个子代理从零开始 |
| Memory | ✅ 继承 | ✅ 继承 |
| 前序发现 | ✅ 自动可用 | ❌ 需显式传入 context |

**最优策略**：有依赖关系→直线执行（复利优先）；完全独立→delegate 并行（速度优先）。

## 参见
- [[agent-usage-patterns]] — Agent 使用模式
- [[agent-memory]] — 记忆架构
- [[llm-fundamentals]] — LLM 基础认知

^[raw/articles/llm-agent-compound-interest.md]
